/**
 * Main content column:
 * - Small screens: full width with `px-2` horizontal padding (no 80% — avoids wasted side space on phones).
 * - `md` and up: 80% width, centered; side margins replace inner padding.
 */
export const SITE_CONTENT_CLASS =
  "w-full min-w-0 mx-auto px-2 md:w-4/5 md:max-w-full md:px-0";
